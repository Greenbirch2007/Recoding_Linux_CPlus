

第1章  开始

类型，变量，表达式，语句及函数

学习一门新的程序设计语言的最好方法就是练习编写程序

1.1  编写一个简答的c++程序

每个c++程序都包含一个或多个函数,
其中一个必须命名为main函数。操作系统通过调用main来运行c++程序。

int main()
{
    return 0;
}
一个函数的定义包含四部分：
返回类型(return type)
函数名(function name)
一个括号包围的形参列表(parameter list,允许为空)
函数体(function body)


main函数的返回类型必须为int,即整数类型。
int 类型是一种内置类型(built-in type),即语言自身定义的类型

在大多数系统中，main的返回值被用来指示状态。
返回值0被用来指示状态。返回值0表明成功，非0的返回值
的含义由系统定义，通常用来指出错误类型。


1.1.1 编译，运行程序


程序源文件命名约定

从命令行运行编译器

访问main的返回值的方法依赖于系统



p31   1.2 初识输入输出